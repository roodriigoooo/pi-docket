node_modules/
npm-debug.log*
.DS_Store
*.tgz
dist/
coverage/
.test-build/

graphify_out/

# personal stress-test sample logs + results (not for the package)
/tmp/docket-stress-*.log
/.stress-results.md

# local-only scripts (not shipped)
scripts/

# local design note for the separate pi-attune theme extension (not part of this package)
/pi-attune-plan.md

# local session working notes (UI/tmux/worker-flow improvement plan)
/PLAN.md

# local session review notes for the core-issues branch
/REVIEW-core-issues.md

# local CLI tool state (not part of this package)
.antigravitycli/

# local Pi runtime state (scrutiny records etc.) written by the installed extension
/.pi/

# raw demo recordings; README uses optimized files in .github/media/
video*_docket.mov

# disposable local fixtures and isolated Pi state for the manual demo guides
/.docket-demo/
/docs/manual-demo.md
/docs/manual-demo-messaging.md
# recording scripts for the README clips: local production notes, not user docs
/docs/readme-demo.md
