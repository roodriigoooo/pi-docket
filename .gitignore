node_modules/
npm-debug.log*
.DS_Store
*.tgz
dist/
coverage/
.test-build/

graphify_out/

# personal stress-test sample logs + results (not for the package)
/tmp/docket-stress-*.log
/.stress-results.md

# local-only scripts (not shipped)
scripts/

# local design note for the separate pi-attune theme extension (not part of this package)
/pi-attune-plan.md

# local session working notes (UI/tmux/worker-flow improvement plan)
/PLAN.md

# local CLI tool state (not part of this package)
.antigravitycli/

# raw demo recordings; README uses optimized files in .github/media/
video*_docket.mov

# disposable local fixture and isolated Pi state for docs/manual-demo.md
/.docket-demo/
